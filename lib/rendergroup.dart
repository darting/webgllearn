part of compass;

class RenderGroup {
  RenderingContext gl;
  
  RenderGroup(this.gl);
  
  addDisplayObjectAndChildren(child) {
    //TODO
  }
  
  removeDisplayObjectAndChildren(displayObject) {
    //TODO
  }
}